<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include('_bootstrap_meta')%>
		<title>Home</title>
		<style></style>
	</head>

	<body style="background-color: #FFFDD0;">
		<%- include('_header') %>
		<div class="w-75 mx-auto mt-5">
			<br/>

			<div class="row">
				<div class="col-6">
					<h2>Databases 1:</h2>
				</div>

				<div class="col-6 d-flex justify-content-end">
					<a href="/students/create" type="button" class="btn btn-success align-self-center"><strong>+</strong> Add User</a>
				</div>
			</div>

			<% if(students.length > 0) {%>
			<div class="mt-3 mb-3" >
				<table class="table table-bordered table-striped" style="background: rgba(255, 255, 255,0.7);">
					<thead class="thead-dark">
						<tr>
							<th scope="col">NAME</th>
							<th scope="col">E-MAIL</th>
							<th scope="col">CONTACT</th>
						</tr>
					</thead>

					<tbody id="myTable">
						<% for(let student of students) {%>
						<tr>
							<td><%=student.name%></td>
							<td><%=student.email%></td>
							<td><%=student.contactNumber%></td>
						</tr>
						<%}%>
					</tbody>
				</table>
			</div>
			<%}else{%>
			<div class="w-75 mx-auto mt-5">
				<h2>No Records Found</h2>
			</div>
			<%}%>
		</div>
		<div class="w-75 mx-auto mt-5">
			<br/>

			<div class="row">
				<div class="col-6">
					<h2>Databases 2:</h2>
				</div>
			</div>

			<% if(students.length > 0) {%>
			<div class="mt-3 mb-3" >
				<table class="table table-bordered table-striped" style="background: rgba(255, 255, 255,0.7);">
					<thead class="thead-dark">
						<tr>
							<th scope="col">NAME</th>
							<th scope="col">E-MAIL</th>
							<th scope="col">CONTACT</th>
						</tr>
					</thead>

					<tbody id="myTable">
						<% for(let student of students) {%>
						<tr>
							<td><%=student.name%></td>
							<td><%=student.email%></td>
							<td><%=student.contactNumber%></td>
						</tr>
						<%}%>
					</tbody>
				</table>
			</div>
			<%}else{%>
			<div class="w-75 mx-auto mt-5">
				<h2>No Records Found</h2>
			</div>
			<%}%>
				
		</div>
		<div class="w-75 mx-auto mt-5">
			<br/>

			<div class="row">
				<div class="col-6">
					<h2>Databases 3:</h2>
				</div>
			</div>

			<% if(students.length > 0) {%>
			<div class="mt-3 mb-3" >
				<table class="table table-bordered table-striped" style="background: rgba(255, 255, 255,0.7);">
					<thead class="thead-dark">
						<tr>
							<th scope="col">NAME</th>
							<th scope="col">E-MAIL</th>
							<th scope="col">CONTACT</th>
						</tr>
					</thead>

					<tbody id="myTable">
						<% for(let student of students) {%>
						<tr>
							<td><%=student.name%></td>
							<td><%=student.email%></td>
							<td><%=student.contactNumber%></td>
						</tr>
						<%}%>
					</tbody>
				</table>
			</div>
			<%}else{%>
			<div class="w-75 mx-auto mt-5">
				<h2>No Records Found</h2>
			</div>
			<%}%>
				
		</div>
		<%- include('_bootstrap_script')%>
	</body>
</html>
